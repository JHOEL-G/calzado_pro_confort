"use client"
import axios from "axios"
import { z } from "zod"
import { FormularioProps } from "./Formulario.type"

export function Formulario(props: FormularioProps) {
    const { conpany } = props
    return (
        <div> </div>
    )
}
